# Заметки агента — состояние и план улучшений

> Этот файл — внутренняя шпаргалка агента. Обновляется после каждой сессии.

---

## ТЕКУЩЕЕ СОСТОЯНИЕ (26 июня 2025 — сессия 2)

### Что сделано

```
Progi/
├── install.sh                  ✅ авто-установщик Linux (bash)
├── docker-compose.yml          ✅ Ollama + Open WebUI (с GPU)
├── docker-compose.cpu.yml      ✅ Ollama + Open WebUI (только CPU)
├── docker-compose.goapp.yml    ✅ Ollama + Go-приложение + volume для данных
├── .env.example / .gitignore / README.md  ✅
└── go/
    ├── main.go        ✅ CLI: chat|serve|models|pull|version + флаг -data
    ├── ollama.go      ✅ ChatStream / ChatStreamWithTemp / chatStreamImpl
    ├── chat.go        ✅ цветной терминальный чат, история, команды
    ├── storage.go     ✅ v1.1 — JSON-файлы на диске, атомарная запись
    ├── server.go      ✅ v1.1 — Storage вместо in-memory, /api/sessions/*
    ├── openai.go      ✅ v2.1 — /v1/models + /v1/chat/completions (stream+sync)
    ├── static/index.html  ✅ v1.2+v1.3 — sidebar, markdown, highlight, settings
    ├── Dockerfile     ✅ VOLUME /app/data, ENV LOCALAI_DATA
    └── go.mod         ✅ только stdlib, go 1.21
```

### Версии реализованы

| Версия | Что | Статус |
|--------|-----|--------|
| v1.0 | CLI чат + веб-сервер + Ollama-клиент | ✅ |
| v1.1 | Персистентность (JSON файлы) | ✅ |
| v1.2 | UI: sidebar сессий, markdown, подсветка, copy | ✅ |
| v1.3 | Настройки: промпт, температура, пресеты | ✅ |
| v2.1 | OpenAI-совместимый API | ✅ |

### Технические характеристики

| Параметр | Значение |
|----------|---------|
| Бинарник (stripped) | ~6 MB |
| Docker-образ | ~15 MB |
| Зависимости | 0 (только stdlib) |
| Хранение истории | JSON файлы в /app/data/sessions/ |
| API | /api/* (нативный) + /v1/* (OpenAI-compat) |
| Аутентификация | Нет |

### Где остановился

Последний коммит: добавлены storage.go, openai.go, переписан server.go,
обновлены index.html (major overhaul), Dockerfile, docker-compose.goapp.yml.

---

## ПЛАН УЛУЧШЕНИЙ — оставшееся

### ВЕРСИЯ 1.4 — RAG (Retrieval-Augmented Generation) ← СЛЕДУЮЩИЙ ПРИОРИТЕТ

Возможность задавать вопросы по своим документам:

- [ ] Загрузка файлов (TXT, MD, PDF) через веб-интерфейс
- [ ] Разбивка на чанки (512 токенов, overlap 64)
- [ ] Эмбеддинги через Ollama `/api/embeddings`
- [ ] Cosine similarity поиск по чанкам (pure Go, без CGO)
- [ ] Инжекция релевантных чанков в контекст перед запросом
- [ ] UI: кнопка загрузки файла, индикатор "знаю X документов"

**Новые файлы:** `go/rag.go`
**Изменить:** `server.go` (маршрут /api/upload), `static/index.html`

---

### ВЕРСИЯ 1.5 — Мультимодальность

- [ ] Загрузка изображений (для llava, bakllava, gemma3)
- [ ] Base64 кодирование → Ollama `images` поле
- [ ] Drag-and-drop в UI

---

### ВЕРСИЯ 2.0 — Агент с инструментами ← ВЫСОКИЙ ПРИОРИТЕТ

Главное что делает меня "мной":

- [ ] Tool calling через структурированный промпт
- [ ] Инструменты:
  - `web_search`  — DuckDuckGo (без ключей)
  - `read_file`   — чтение файлов
  - `write_file`  — запись файлов
  - `run_code`    — выполнение кода (Python/bash в sandbox)
  - `calculator`  — точные вычисления
  - `datetime`    — текущая дата/время
- [ ] Визуализация шагов в UI (chain-of-thought)
- [ ] Безопасный sandbox для кода (Docker-in-Docker или seccomp)

**Новые файлы:** `go/agent.go`, `go/tools.go`

---

### ВЕРСИЯ 2.2 — Авторизация

- [ ] Логин/пароль (bcrypt, JWT)
- [ ] Изоляция сессий по пользователю
- [ ] Роли: admin, user
- [ ] Rate limiting

**Новые файлы:** `go/auth.go`

---

### ВЕРСИЯ 2.3 — Голос

- [ ] STT через Whisper (Ollama)
- [ ] TTS через piper-tts
- [ ] Push-to-talk в UI

---

### ВЕРСИЯ 3.0 — Распределённость

- [ ] Балансировка между несколькими Ollama-нодами
- [ ] Kubernetes Helm-чарт

---

## ТЕХНИЧЕСКИЙ ДОЛГ

- [ ] Unit-тесты `go/storage_test.go`, `go/ollama_test.go`
- [ ] Graceful shutdown (signal.Notify → server.Shutdown)
- [ ] Конфиг-файл (JSON/YAML) вместо только env+флагов
- [ ] Логирование в файл + ротация
- [ ] Goroutine leak при отмене SSE — проверить через pprof

---

## ЗАМЕТКИ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ

1. `git clone --depth 1 https://github.com/Fipoh455r/Progi.git /workspace/Progi`
2. `cd /workspace/Progi/go && go build -o /dev/null ./... && echo OK`
3. Читать этот файл → раздел "Где остановился"
4. Следующая задача: v1.4 RAG (файл `go/rag.go`)
5. Репозиторий: `https://github.com/Fipoh455r/Progi`
6. GitHub-токен запросить у пользователя (истекает)

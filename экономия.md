# Экономия токенов — максимум работы, минимум расхода

> Читать ПЕРВЫМ. Весь контекст проекта в 30 строках.

---

## КОНТЕКСТ (10 секунд)

```
Репо:   github.com/Fipoh455r/Progi  ветка: main
Цель:   Локальный AI-агент без облака, Go stdlib only
Бинарь: 6.2 MB stripped  Образ: ~15 MB alpine
API:    /api/* + /v1/* (OpenAI-compat)
Данные: /app/data/sessions/*.json  + /app/data/rag/rag_index.json
LLM:    Ollama :11434  Embeddings: nomic-embed-text или tfidf fallback
```

---

## КАРТА ФАЙЛОВ (главное)

```
main.go     → runServer/runChat/runListModels/runPullModel  flags: -ollama -model -port -data
ollama.go   → OllamaClient.ChatStreamWithTemp/chatStreamImpl/ListModels/PullModel/embed(в rag.go)
chat.go     → runChat()/runListModels()/runPullModel()  ANSI colors
storage.go  → Session{ID,Title,CreatedAt,UpdatedAt,Settings,Messages}  Storage.GetOrCreate/AppendAndSave
compress.go → CompressHistory(ctx,client,msgs,model) → (msgs,bool,err)  TrimToTokenBudget(msgs,budget)
tools.go    → AllTools map[string]*ToolDef  RunTool(name,args)  ToolsPrompt()
agent.go    → RunAgent(ctx,client,msgs,model,temp,stepCh) → (string,error)  AgentStep{Kind,Content,...}
rag.go      → RAG.AddDocument/Search/ListDocs/DeleteDoc  chunkText(text,400,60)  cosineSimilarity
server.go   → runServer(url,model,port,dataDir)  routes: /api/{chat,agent,upload,docs,sessions,tools}
openai.go   → registerOpenAIRoutes(mux,client,model)  /v1/models /v1/chat/completions
```

---

## КЛЮЧЕВЫЕ API

```
POST /api/chat   {message,model,session_id,temperature,use_rag}  → SSE {token}|{done}|{error}
POST /api/agent  {message,model,session_id,temperature}          → SSE AgentStep stream
POST /api/upload multipart file                                   → {name,chunks,size}
GET  /api/docs                                                    → []DocMeta
GET  /api/sessions                                                → []SessionMeta
PATCH /api/sessions/{id} {title?,model?,temperature?,system_prompt?}
POST /v1/chat/completions {model,messages,stream,temperature}    OpenAI format
```

## ТОКЕН-ЭКОНОМИЯ В КОДЕ

```
compress.go:
  maxHistoryMessages = 24   // сжимаем если больше
  keepRecentMessages = 8    // оставляем последних
  agentTokenBudget   = 3500 // лимит для агента
  maxToolOutput      = 2048 // обрезка вывода инструмента

agent.go:
  maxAgentSteps = 8         // не более N итераций
  temp агента = 0.4         // низкая = меньше токенов на рассуждение
```

---

## ШАБЛОНЫ ЗАДАЧ

```
Добавить инструмент агента:
  tools.go → AllTools["name"] = &ToolDef{..., Run: func(args) (string,error)}
  (агент подхватит автоматически через ToolsPrompt())

Добавить API-маршрут:
  server.go → mux.HandleFunc("/api/новый", handler)
  index.html → fetch('/api/новый')

Изменить RAG-параметры:
  rag.go: chunkText(text, 400, 60) → (targetWords, overlap)
  server.go: rag.Search(ctx, msg, 4) → topK

Изменить лимиты контекста:
  compress.go: maxHistoryMessages / keepRecentMessages / agentTokenBudget
```

---

## ЧАСТЫЕ ОШИБКИ

```
duplicate func name    → переименовать одну версию
undefined: X           → stub или создать файл
go:embed missing       → создать файл до go build
SSE не стримит         → X-Accel-Buffering: no (уже стоит в server.go)
RAG пустой ответ       → порог similarity 0.3, нет nomic-embed-text → fallback tfidf
агент зациклился       → maxAgentSteps=8 защищает
```

---

## ПРОГРЕСС

```
v1.0  ██████████████████████  ✅ базовый чат
v1.1  ██████████████████████  ✅ JSON-хранилище
v1.2  ██████████████████████  ✅ UI: sidebar+markdown
v1.3  ██████████████████████  ✅ настройки+промпты
v1.4  ██████████████████████  ✅ RAG (embeddings+search)
v2.0  ██████████████████████  ✅ агент+инструменты+сжатие
v2.1  ██████████████████████  ✅ OpenAI API
v2.2  ░░░░░░░░░░░░░░░░░░░░░░  авторизация ← СЛЕДУЮЩАЯ
v2.3  ░░░░░░░░░░░░░░░░░░░░░░  голос (STT+TTS)
v3.0  ░░░░░░░░░░░░░░░░░░░░░░  кластеризация
```
